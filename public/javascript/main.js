// Generated by CoffeeScript 1.6.3
(function() {
  require.config({
    waitSeconds: 60,
    onError: function(requireType, requireModules) {
      if (requireType === 'timeout') {
        return alert('Twitter-io Module: Your connection seems to be very slow at the moment.\nPlease try again later.');
      }
    },
    paths: {
      jquery: "/twitterio/components/jquery/jquery",
      lodash: "/twitterio/components/lodash/lodash",
      backbone: "/twitterio/components/backbone/backbone",
      marionette: "/twitterio/components/backbone.marionette/lib/backbone.marionette",
      bootstrap: "/twitterio/components/bootstrap/js/bootstrap",
      io: "/socket.io/socket.io",
      tweet: "models/Tweet",
      tweets: "collections/Tweets",
      tweetsView: "views/tweetsView",
      tweetView: "views/tweetView"
    },
    shim: {
      jquery: {
        exports: '$'
      },
      lodash: {
        exports: '_'
      },
      backbone: {
        deps: ['lodash', 'jquery'],
        exports: "Backbone"
      },
      bootstrap: {
        exports: 'Bootstrap',
        deps: ['jquery']
      },
      marionette: {
        exports: 'Marionette',
        deps: ['bootstrap']
      },
      io: {
        exports: 'io'
      }
    }
  });

  define(function(require) {
    var $, Backbone, Marionette, app, _;
    $ = require('jquery');
    Backbone = require('backbone');
    _ = require('lodash');
    Marionette = require('marionette');
    app = require('app');
    return $(function() {
      return app.start();
    });
  });

}).call(this);
